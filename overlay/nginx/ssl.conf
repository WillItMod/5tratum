# Legacy file (unused).
#
# IMPORTANT: nginx auto-loads conf.d/*.conf at the http{} level. A file containing
# bare `listen 443 ssl;` directives will crash nginx ("listen directive is not allowed here").
#
# 5tratumOS now writes `/etc/nginx/conf.d/ssl.inc` and `default.conf` includes it
# inside the `server {}` block when HTTPS is enabled.
